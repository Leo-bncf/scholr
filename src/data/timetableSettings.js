import { supabase } from '@/lib/supabase';
import { rows, maybeOne, one, none } from './_query';

/**
 * timetable_settings
 *
 * GENERATED by scripts/gen-data-modules.mjs. Safe to edit — regeneration skips
 * any module listed as hand-written in that script, so promote this file there
 * before making substantial changes.
 *
 * RLS decides what a caller can see; these functions do not re-check roles.
 */

const COLUMNS = 'id, school_id, external_system_name, external_system_url, external_school_id, sync_enabled, override_policy, sync_frequency, field_mapping, id_mappings, use_rotating_cycle, cycle_days, locked_fields, show_room_to_students, show_teacher_to_students, link_attendance_to_schedule, last_sync_at, last_sync_status, created_at';

/** Everything in a school. */
export function listForSchool(schoolId) {
  return rows(
    supabase.from('timetable_settings').select(COLUMNS).eq('school_id', schoolId)
      .order('created_at', { ascending: false }),
    'timetableSettings.listForSchool',
  );
}

export function get(id) {
  return maybeOne(supabase.from('timetable_settings').select(COLUMNS).eq('id', id), 'timetableSettings.get');
}

/**
 * Equality filters, for call sites that don't have a named query yet.
 *
 * `where({ school_id, status })` maps each key to `.eq()`; an array value
 * becomes `.in()` and null becomes `.is(null)`. Prefer adding a named
 * function above when the same filter shows up more than once.
 */
export function where(filters = {}, { order, ascending = false, limit } = {}) {
  let q = supabase.from('timetable_settings').select(COLUMNS);
  for (const [key, value] of Object.entries(filters)) {
    if (value === undefined) continue;
    if (value === null) q = q.is(key, null);
    else if (Array.isArray(value)) q = q.in(key, value);
    else q = q.eq(key, value);
  }
  if (order) q = q.order(order, { ascending });
  if (limit) q = q.limit(limit);
  return rows(q, 'timetableSettings.where');
}

export function create(record) {
  return one(supabase.from('timetable_settings').insert(record).select(COLUMNS), 'timetableSettings.create');
}

export function update(id, patch) {
  return one(
    supabase.from('timetable_settings').update(patch).eq('id', id).select(COLUMNS),
    'timetableSettings.update',
  );
}

export function remove(id) {
  return none(supabase.from('timetable_settings').delete().eq('id', id), 'timetableSettings.remove');
}
