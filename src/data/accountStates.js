import { supabase } from '@/lib/supabase';
import { rows, maybeOne, one, none } from './_query';

/**
 * account_states
 *
 * GENERATED by scripts/gen-data-modules.mjs. Safe to edit — regeneration skips
 * any module listed as hand-written in that script, so promote this file there
 * before making substantial changes.
 *
 * RLS decides what a caller can see; these functions do not re-check roles.
 */

const COLUMNS = 'id, user_id, user_email, account_status, school_id, school_name, role, invitation_token, invitation_accepted_at, first_login_at, last_login_at, profile_completed, profile_completion_required_fields, password_reset_token, password_reset_expires_at, password_set_at, suspended_reason, suspended_at, suspended_by, email_verified, email_verified_at, login_attempts, locked_until, onboarding_step, metadata, created_at';

/** Everything in a school. */
export function listForSchool(schoolId) {
  return rows(
    supabase.from('account_states').select(COLUMNS).eq('school_id', schoolId)
      .order('created_at', { ascending: false }),
    'accountStates.listForSchool',
  );
}

export function get(id) {
  return maybeOne(supabase.from('account_states').select(COLUMNS).eq('id', id), 'accountStates.get');
}

/**
 * Equality filters, for call sites that don't have a named query yet.
 *
 * `where({ school_id, status })` maps each key to `.eq()`; an array value
 * becomes `.in()` and null becomes `.is(null)`. Prefer adding a named
 * function above when the same filter shows up more than once.
 */
export function where(filters = {}, { order, ascending = false, limit } = {}) {
  let q = supabase.from('account_states').select(COLUMNS);
  for (const [key, value] of Object.entries(filters)) {
    if (value === undefined) continue;
    if (value === null) q = q.is(key, null);
    else if (Array.isArray(value)) q = q.in(key, value);
    else q = q.eq(key, value);
  }
  if (order) q = q.order(order, { ascending });
  if (limit) q = q.limit(limit);
  return rows(q, 'accountStates.where');
}

export function create(record) {
  return one(supabase.from('account_states').insert(record).select(COLUMNS), 'accountStates.create');
}

export function update(id, patch) {
  return one(
    supabase.from('account_states').update(patch).eq('id', id).select(COLUMNS),
    'accountStates.update',
  );
}

export function remove(id) {
  return none(supabase.from('account_states').delete().eq('id', id), 'accountStates.remove');
}
