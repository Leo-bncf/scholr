import { supabase } from '@/lib/supabase';
import { rows, maybeOne, one, none } from './_query';

/**
 * gradebook_policies
 *
 * GENERATED by scripts/gen-data-modules.mjs. Safe to edit — regeneration skips
 * any module listed as hand-written in that script, so promote this file there
 * before making substantial changes.
 *
 * RLS decides what a caller can see; these functions do not re-check roles.
 */

const COLUMNS = 'id, school_id, grading_model, categories, allow_teacher_category_override, default_visible_to_student, default_visible_to_parent, feedback_only_mode, grade_release_mode, scheduled_release_date, allow_teacher_visibility_override, reporting_windows, lock_grades_after_deadline, require_justification_for_locked_edit, justification_min_chars, admin_can_override_lock, predicted_grades_enabled, predicted_grade_entry_roles, predicted_grades_require_rationale, coordinator_can_lock_predicted, predicted_grades_locked, predicted_grades_released_to_student, predicted_grades_released_to_parent, created_at';

/** Everything in a school. */
export function listForSchool(schoolId) {
  return rows(
    supabase.from('gradebook_policies').select(COLUMNS).eq('school_id', schoolId)
      .order('created_at', { ascending: false }),
    'gradebookPolicies.listForSchool',
  );
}

export function get(id) {
  return maybeOne(supabase.from('gradebook_policies').select(COLUMNS).eq('id', id), 'gradebookPolicies.get');
}

/**
 * Equality filters, for call sites that don't have a named query yet.
 *
 * `where({ school_id, status })` maps each key to `.eq()`; an array value
 * becomes `.in()` and null becomes `.is(null)`. Prefer adding a named
 * function above when the same filter shows up more than once.
 */
export function where(filters = {}, { order, ascending = false, limit } = {}) {
  let q = supabase.from('gradebook_policies').select(COLUMNS);
  for (const [key, value] of Object.entries(filters)) {
    if (value === undefined) continue;
    if (value === null) q = q.is(key, null);
    else if (Array.isArray(value)) q = q.in(key, value);
    else q = q.eq(key, value);
  }
  if (order) q = q.order(order, { ascending });
  if (limit) q = q.limit(limit);
  return rows(q, 'gradebookPolicies.where');
}

export function create(record) {
  return one(supabase.from('gradebook_policies').insert(record).select(COLUMNS), 'gradebookPolicies.create');
}

export function update(id, patch) {
  return one(
    supabase.from('gradebook_policies').update(patch).eq('id', id).select(COLUMNS),
    'gradebookPolicies.update',
  );
}

export function remove(id) {
  return none(supabase.from('gradebook_policies').delete().eq('id', id), 'gradebookPolicies.remove');
}
