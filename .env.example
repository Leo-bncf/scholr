# Copy this file to .env.local and fill in the real value.
# The anon key only grants the anonymous Postgres role (row-level security
# does the real auth). Ask Leo for it if you don't have it yet.

VITE_SUPABASE_URL=https://api.scholr.pro
VITE_SUPABASE_ANON_KEY=paste-the-anon-key-here